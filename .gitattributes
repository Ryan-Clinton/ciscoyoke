# Normalise to LF in the repository regardless of the checkout platform.
* text=auto eol=lf

# Transcripts are byte-exact recordings of a serial console. Any line-ending
# translation would corrupt them and break replay.
*.ytx binary
*.ytx.pub binary

*.docx binary
*.png binary
*.bin binary
